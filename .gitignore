runs/
.env
.env.*
*.json.bak

# GCP credentials
*.json
!batch/batch_config.example.json
!batch/test-scripts/script.json
!tests/fixtures/*.json
!tests/e2e_scripts/*.json
!docs/workflows/**/*.json
!web/**/*.json
!web/**/*.jsonc

# E2E generation log (output of tests/e2e_log.py)
tests/e2e_generation_log.jsonl

# Per-project working assets: reference images, extracted frames, scratch
# audio, and pipeline output kept next to the timeline that produced it. Same
# category as runs/ — generated or one-off, not source. They stay on disk; git
# just doesn't carry them.
assets/*/
!assets/*.jpeg
!assets/*.jpg
!assets/*.png

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Sisyphus sessions
.sisyphus/

# Claude Code
.claude/

# Downloaded videos & watch outputs
*.mp4
!tests/fixtures/*.mp4
watch-outputs/

# Strategy & scene export artifacts
strategy_output/
scene_export_*/

# Asset subsets
assets/spin-quest-imgs
assets/makeover-imgs
assets/makeover-original-imgs
assets/royal-match-imgs
assets/coins-images
# Python build artifacts
mograf.egg-info/
*.egg-info/
__pycache__/
*.egg

# Codex local config (machine-specific MCP setup)
.codex/
